# 可研究课题

以下课题围绕任务生成、策略训练、失败分析、自我修改和验证更新展开。每个课题都可以实现为规模受控、可回滚的实验系统，适合作为课程项目或进一步扩展为研究工作。

| 课题 | 研究问题 | 可以采用的思路 | 主要指标 | 难度 |
|---|---|---|---|---|
| **1. 面向奖励投机的红皇后验证器** | 在自修改过程中，固定验证器是否会使 Agent 通过过拟合测试或利用评测漏洞获得虚假的性能提升？持续生成针对性测试，能在多大程度上缩小评测得分与真实泛化能力之间的差距？ | 选择可自动执行的 Python 编程任务；由求解器提交程序，由挑战者根据历史错误生成边界测试；分别采用固定测试、随机增广测试、对抗测试和相互独立的双验证器；保留一组始终不向 Agent 暴露的隐藏测试。 | 隐藏测试通过率、公开—隐藏分数差距、奖励投机率、回退次数 | ★★★ |
| **2. 可回滚的工程化 Gödel 机** | 在评测存在随机波动和任务差异的条件下，哪种修改准入规则能够更准确地识别真实改进，并减少新版本造成的能力回退？ | 将可修改范围限制为提示词、记忆策略、工具路由和少量辅助代码；为每次修改保存独立版本；尝试平均分门控、配对检验、置信下界门控和多目标帕累托门控；未通过审核的版本自动回滚。 | 修改成功率、回归率、累计能力增长、计算成本、安全违规率 | ★★★ |
| **3. 基于学习进展的红皇后课程生成** | 对任务生成器而言，奖励“当前求解失败”还是奖励“求解器经过训练后的能力增量”，更有利于形成难度适中且持续推进能力边界的课程？ | 使用带参数的任务模板或领域专用语言生成可验证的算法题；让挑战者根据求解历史调整任务难度；考察固定课程、随机课程、以失败率为奖励的课程和以学习进展为奖励的课程；控制各组的训练任务数和推理预算。 | 学习曲线面积、样本效率、任务难度校准、任务多样性 | ★★★ |
| **4. 失败记忆与灾难性遗忘** | 在任务分布逐步变化时，哪些失败经验值得写入长期记忆？不同的记忆选择和淘汰策略如何影响新任务迁移、旧任务保持以及错误复发？ | 将任务划分为若干连续阶段；记录失败轨迹、错误类型和修复结果；尝试不保存记忆、保存完整轨迹、仅保存近期错误、保存聚类后的失败模式，以及按使用价值淘汰记忆；定期回测早期任务。 | 前向迁移、后向迁移、遗忘率、上下文开销、错误复发率 | ★★☆ |
| **5. 单一路径与进化档案的比较** | 在相同模型调用和评测预算下，保留多个历史 Agent 是否比只沿当前最优版本继续修改更容易发现有效改进，并降低搜索停滞和能力遗忘？ | 实现一个受限的 DGM-lite；每个版本记录父节点、修改内容和评测结果；尝试贪心单链、随机档案采样、基于新颖性的采样，以及兼顾质量和多样性的档案维护方法；在多个任务族上复测历史版本。 | 最佳性能、种群多样性、停滞轮数、跨任务鲁棒性 | ★★★☆ |
| **6. 自评估循环何时应该停止** | 当 Judge 同时产生训练奖励并通过这些奖励改进自身时，奖励信号会在什么条件下开始偏离外部评价？能否利用独立监测信号在性能退化前确定停止轮次？ | 让 Judge 评价开放式回答并根据自生成反馈更新；增加一个参数或提示独立的 Checker；尝试固定轮数、外部验证集早停、Judge—Checker 一致性早停和奖励有效性早停；保留少量人工或规则标注作为审计集。 | Judge 准确率、校准误差、奖励—真实表现相关性、最优停止轮次 | ★★★☆ |
| **7. 基于失败归因的自修改选择器** | 根据执行轨迹判断失败来自提示词、记忆检索、工具调用还是程序逻辑，是否能够提高修改命中率，并减少无效修改对其他能力的破坏？ | 构造带有已知故障来源的任务；让 Agent 输出故障归因和修改对象；尝试随机选择组件、固定修改顺序、基于 LLM 的轨迹诊断，以及根据历史修复收益学习修改策略；修改后同时测试目标任务和回归任务。 | 归因准确率、有效修改率、修复成本、误修改率 | ★★☆ |
| **8. 同源自评与异构同伴评估** | 生成器与评估器使用相同模型或同一模型系列时，是否更容易出现相关错误、奖励虚高和训练坍缩？在相同评测成本下，引入异构评估器能改善多少奖励可靠性？ | 比较单模型自评、同模型多次采样、同系列双模型互评和不同模型系列互评；使用带标准答案的任务估计评估误差；进一步观察这些奖励用于多轮策略更新后是否造成错误累积。 | 奖励准确率、排序一致性、错误相关性、训练坍缩率、API 成本 | ★★★ |

难度以完成一个可运行闭环并进行基本消融实验为基准：★★☆表示工作量较可控，★★★表示需要较完整的系统实验，★★★☆表示还需处理多轮训练或较复杂的实验控制。
